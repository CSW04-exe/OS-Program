# OS-Program
This program simulates a priority-based process scheduler using a custom synchronized priority queue. It reads process specifications from standard input, creates process objects with attributes such as ID, name, runtime, class, and priority, and assigns them to a shared queue.
